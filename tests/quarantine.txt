# Tests whose failure is known, understood, and not the fault of whatever landed
# last. A quarantined test still runs and still reports; it just does not fail
# the build. One line per scene path, `#` for comments.
#
# Delete a line the moment its test goes green — a quarantine nobody removes is
# just a test nobody reads.

# The cycle curve peaks at one overclock (0.035 -> 0.030 -> 0.039), so the
# second one is already past the top and `c[2] < c[1]` cannot hold. The other
# twelve checks in the file pass, including `best = 1`. This is the tuning and
# the assertion disagreeing about where the sweet spot should be, not a
# regression: it has failed since at least 51504f9. Settle the balance, then
# delete this line.
tests/feature/overclock_test.tscn
